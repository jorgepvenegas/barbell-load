{
  "name": "barbell-load-calculator",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for Barbell Load Calculator",
  "scripts": {
    "dev:web": "pnpm --filter @barbell/web dev",
    "dev:mobile": "pnpm --filter mobile start",
    "build:web": "pnpm --filter @barbell/web build",
    "build:all": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
